#ifndef SRC_UTILS_H
#define SRC_UTILS_H
#include <algorithm>
#include <cctype>
#include <cstring>
#include <string>

using std::string;

namespace spider {
string trim(const string& s);
}  // namespace spider
#endif